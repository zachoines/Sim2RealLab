# strafer_driver default parameters
# Override via launch file or command line.

roboclaw_node:
  ros__parameters:
    front_port: "/dev/roboclaw0"
    rear_port: "/dev/roboclaw1"
    baud_rate: 115200
    publish_rate: 50.0
    auto_detect: true  # Probe ports to find front/rear by address
